# Music beds

Drop any `.mp3` here — the station plays what it finds.

- Regenerate the default synth tracks with `node scripts/make-music.mjs` (requires `ffmpeg` on PATH).
- For the hackathon you may replace these with royalty-free tracks, e.g. from [pixabay.com/music](https://pixabay.com/music/).
